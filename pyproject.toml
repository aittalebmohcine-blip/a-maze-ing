[project]
name = "mazegen"
version = "1.0.0"
description = "Maze generator library"
authors = [{name = "your_login"}]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mazegen*"]
